#!/bin/bash
# Global 'generate' command – runs the credential generator
DIR="$(cd "$(dirname "$(readlink -f "$0" 2>/dev/null || echo "$0")")" && pwd)"
exec "$DIR/../venv/bin/python" "$DIR/../main.py" "$@"
